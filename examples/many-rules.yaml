rules:
    - contains: .git/
    - contains: hs
    - contains: test/
    - in: ~/Code
actions:
    -
        binding: f
        title: foo
        description: Foo some bar
        shell: echo foo!
    -
        binding: p
        title: papa
        description: Papa Charlie
        shell: echo PC
